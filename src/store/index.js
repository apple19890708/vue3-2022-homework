import { createStore } from 'vuex';
import commonModule from './commonModule';

export default createStore({
  strict: true,
  state: {
  },
  actions: {
  },
  mutations: {
  },
  modules: {
    commonModule
  },
});
